<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <title>App</title>
</head>

<body>
<div class="container">
    <h1>URL Shortener</h1>
    <form action="/shortener" method="POST" class="my-4 form-inline">
        <label for="longUrl" class="sr-only"></label>
        <input required placeholder="URL" type="url" name="longUrl" id="longUrl" class="form-control mr-2 col">
        <button class="btn btn-success mt-2" type="submit">Shortener</button>
    </form>
    <table class="table table-striped table-responsive">
        <thead>
        <tr>
            <th>Full URL</th>
            <th>Short URL</th>
            <th>Url Code</th>
        </tr>
        </thead>
        <tbody>
        {{#each someUrls}}
            <tr>
                <td><a href={{this.longUrl}}>{{this.longUrl}}</a></td>
                <td><a href={{this.shortUrl}}>{{this.shortUrl}}</a></td>
                <td><a href={{this.urlCode}}>{{this.urlCode}}</a></td>
            </tr>
        {{/each}}
        </tbody>
    </table>
</div>
</body>

</html>